<!--代码清单6-35 用ejs模版生成xml-->
 <entries>
    <% entries.forEach((entry)=>{ %>
        <entry>
            <title><%= entry.title %></title>
            <body><%= entry.body %></body>
            <username><%= entry.title %></username>
        </entry>
    <% }) %>
</entries>